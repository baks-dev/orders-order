{% trans_default_domain 'orders-order.admin' %}


{#{{ dump(opens) }}#}

<section class="card card-flush alert alert-warning p-0 w-100 "
         style="background-image: var(--bs-gradient);">

    {# <div class="px-3 pt-3 d-flex gap-3">
        <div class="w-50 d-flex align-items-center gap-3">

            <span class="badge text-bg-danger"> #}{# {{ opens.complete|trans({}, 'manufacture.complete') }} #}{# </span>
    #}{# {{ opens.marketplace|trans({}, 'manufacture.marketplace') }} #}{#
    #}{# <div><small class="opacity-50">Категория:</small> &nbsp; <strong>{{ opens.category_name }}</strong></div> #}{#
    </div>

    <div class="w-100">
        <strong> #}{# {{ opens.category_name }} #}{# : </strong> &nbsp;
            <small class="opacity-50">
                последний добавленный продукт</small>
        </div>

    </div> #}


    <div class="card-body d-flex justify-content-between">

        <div class="w-75 d-flex gap-3 align-items-center">

            <span data-bs-toggle="tooltip"
                  data-bs-placement="bottom"
                  data-bs-delay='{"show":600,"hide":100}'
                  title='QR заказа'
            >
                <a
                        href="{{ path('orders-order:admin.order.qrcode', { id : opens.order_id }) }}"
                        class="btn-link btn p-0 modal-link icon"
                        data-bs-toggle="modal"
                        data-bs-target="#modal"
                        style="height: 55px; width: 55px;"
                >
                    <svg
                            xmlns="http://www.w3.org/2000/svg" width="45" height="45" fill="currentColor"
                            class="bi bi-qr-code" viewBox="0 0 16 16"><path d="M2 2h2v2H2V2Z"></path><path
                                d="M6 0v6H0V0h6ZM5 1H1v4h4V1ZM4 12H2v2h2v-2Z"></path><path
                                d="M6 10v6H0v-6h6Zm-5 1v4h4v-4H1Zm11-9h2v2h-2V2Z"></path><path
                                d="M10 0v6h6V0h-6Zm5 1v4h-4V1h4ZM8 1V0h1v2H8v2H7V1h1Zm0 5V4h1v2H8ZM6 8V7h1V6h1v2h1V7h5v1h-4v1H7V8H6Zm0 0v1H2V8H1v1H0V7h3v1h3Zm10 1h-1V7h1v2Zm-1 0h-1v2h2v-1h-1V9Zm-4 0h2v1h-1v1h-1V9Zm2 3v-1h-1v1h-1v1H9v1h3v-2h1Zm0 0h3v1h-2v1h-1v-2Zm-4-1v1h1v-2H7v1h2Z"></path><path
                                d="M7 12h1v3h4v1H7v-4Zm9 2v2h-3v-1h2v-1h1Z"></path>
                    </svg>
                </a>
            </span>

            <div>

                <div>
                    <small class="opacity-50">Тип заказа:</small> &nbsp;
                    <strong>{{ opens.type_profile_name }}</strong>
                </div>

                <div>
                    <small class="opacity-50">Номер:</small> &nbsp;
                    <a href="{{ path('orders-order:admin.order.products', { id: opens.order_id}) }}"
                       class="fw-bolder text-decoration-none">
                        {{ opens.order_number }}
                    </a>
                </div>

                <div>
                    <small class="opacity-50">Отвественный:</small> &nbsp;
                    <strong>{{ opens.users_profile_username }}</strong>
                </div>

            </div>
        </div>

        {# Последний добавленный продукт #}
        <div id="product-{{ opens.order_event }}" class="w-100 d-flex gap-3 align-items-center justify-content-start">

            {% if opens.product_event %}
                 {{ include('@orders-order/admin/order/add/pc/product.html.twig', { product : opens }) }}
            {% else %}
                <small>В заказе отстутвует продукция</small>
            {% endif %}
        </div>

        <div class="text-end d-flex gap-1 align-items-center">
            <a href="{{ path('orders-order:admin.order.package', { id: opens.order_id }) }}" id="close_supply_btn"
               class="btn modal-btn btn-primary modal-link text-nowrap"
               data-bs-toggle="modal"
               data-bs-target="#modal"
               title="Отпарвить заказ на упаковку"
            >
                Отправить на упаковку
            </a>

            <a href="{{ path('orders-order:admin.order.delete', { id: opens.order_event }) }}"
               class="btn modal-btn btn-danger modal-link text-nowrap"
               data-bs-toggle="modal"
               data-bs-target="#modal"
               title="Отпарвить заказ на упаковку"
            >
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-x-lg" viewBox="0 0 16 16">
                    <path d="M2.146 2.854a.5.5 0 1 1 .708-.708L8 7.293l5.146-5.147a.5.5 0 0 1 .708.708L8.707 8l5.147 5.146a.5.5 0 0 1-.708.708L8 8.707l-5.146 5.147a.5.5 0 0 1-.708-.708L7.293 8z"/>
                </svg>
            </a>
        </div>

        {# <div class="text-end w-25">
            <a href="#"
               class="btn modal-btn modal-link btn-primary" data-bs-toggle="modal" data-bs-target="#modal"
               title="Открыть поставку">
                Открыть партию
            </a>
        </div> #}

    </div>
</section>