<div class="row">
    <div class="tab-content" id="orders-tabContent">

        {# TAB #1 - BASKET #}
        <div role="tabpanel"
             class="tab-pane fade show active"
             aria-labelledby="orders-actual-tab"
             id="orders-actual">

            <div class="col-12">

                {% if orders.new is defined %}

                    {{ include (_self|replace({ "orders_tabs.html.twig" : "products.html.twig" }), { 'orders': orders.new }, with_context = false) }}

                {% else %}

                    <div class='d-flex justify-content-between align-items-center'>
                        <h2 class="fs-18 mb-0 text-uppercase fw-bold mb-2">
                            Актуальных заказов нет
                        </h2>
                    </div>

                {% endif %}

            </div>

        </div>

        {# TAB #2 - PAYMENT #}
        <div role="tabpanel"
             class="tab-pane fade"
             aria-labelledby="orders-closed-tab"
             id="orders-closed">

            <div class="col-12">

                {% if orders.completed is defined %}

                    {{ include (_self|replace({ "orders_tabs.html.twig" : "products.html.twig" }), { 'orders': orders.completed }, with_context = false) }}

                {% else %}

                    <div class='d-flex justify-content-between align-items-center px-4 py-2 mb-3'>

                        <h2 class="fs-18 mb-0 text-uppercase fw-bold mb-2">
                            Завершенных заказов нет
                        </h2>

                    </div>

                {% endif %}

            </div>
        </div>

    </div>

</div>