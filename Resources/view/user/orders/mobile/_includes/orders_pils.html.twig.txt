<div class="row">
    <div class="col-12">

        {#        PILS #}
        <div class="d-flex align-items-center gap-2 mb-3"
             role="tablist"
             id="tab">

            <button class="btn border fw-bold text-secondary btn-alt rounded-5 active"
                    type="button" role="tab"
                    data-bs-toggle="tab"
                    data-bs-target="#orders-actual"
                    aria-controls="orders-actual"
                    aria-selected="true"
                    id="orders-actual-tab">
                Актуальные
            </button>

            <a href="{{ path('orders-order:user.orders',
                { status: constant('BaksDev\\Orders\\Order\\Type\\Status\\OrderStatus\\OrderStatusNew::STATUS')
                } ) }}"
               class="text-black btn border fs-12 rounded-5">
                {{ orders.new|length }}
            </a>

            <button class="btn border fw-bold text-secondary btn-alt rounded-5"
                    type="button" role="tab"
                    data-bs-toggle="tab"
                    data-bs-target="#orders-closed"
                    aria-controls="orders-closed"
                    aria-selected="false"
                    id="orders-closed-tab">
                Завершенные
            </button>

            <a href="{{ path('orders-order:user.orders',
                { status: constant('BaksDev\\Orders\\Order\\Type\\Status\\OrderStatus\\OrderStatusCompleted::STATUS')
                } ) }}"
               class="text-black btn border fs-12 rounded-5">
                {{ orders.completed|length }}
            </a>

        </div>

        {#        TABS #}
        {#                            {{ include (_self|replace({ "content.html.twig" : "_includes/orders_tabs.html.twig" }), { orders, paginator }, with_context = false) }} #}
        {{ include(_self|replace({ "content.html.twig": "_includes/orders_tabs.html.twig" })) }}

    </div>
</div>
